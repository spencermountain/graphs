Converted from the 2022 svelte original in public/2022/riding-sankey. Ported the 2022 somehow-sankey layout (verified against this post's build/bundle.js — newer than the scratchpad lib source) into sankey.js: per-column percent labels, `after` (hidden name) and `inline` node flags. Skipped features this post never renders: show_num/fmt (false everywhere), `show_label` (collected but unused by the 2022 build), `full` dot-pattern, dx/dy, brace labels, `append`. The original's drop-border exactly covered the node's default pink underline, so the accent is drawn as the node's border-bottom directly. Added a resize re-layout the original lacked. 'Catamaran' font falls back to sans-serif. scripts/ (raw permit data + ward polygons that generated counts.js) copied as-is for provenance; not imported by the page.
