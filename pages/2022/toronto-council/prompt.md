Converted from the 2022 svelte original in public/2022/toronto-council.

Notes: counts.js converted CommonJS→ESM; the old `lib/` and `scripts/` files were offline data-prep and are not ported. The original inlined an invalid `border-left: solid undefined` on colored-less highlights (falling back to lightgrey) — replicated with a conditional style. Legend label colors keep the original's hard-coded hexes, including the Anthony Perruzza mismatch (label #2D85A8 vs bar #2e7794).
