Converted from the 2022 svelte original in public/2022/accumulated-units. Approximations: old `Page` header/footer shell recreated per repo convention; built-bar color `#6699cc` replaced with the identical `colors.blue`; original scoped `.row`/`.container` renamed `.chart-row`/`.chart-container` to avoid clashing with global/Tailwind classes; container `margin-bottom` bumped 3rem→4rem so the axes hanging below the chart clear the card edge.
