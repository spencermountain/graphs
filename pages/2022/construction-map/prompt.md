Converted from the 2022 svelte/js original in public/2022/construction-map.

Notes: ported mapbox-gl → maplibre-gl. The original custom mapbox style (`spencermountain/cl8hsb6pu...`) and token are dead, so the basemap (faint streets) is replaced by a plain `#fbfbfb` background — the same color the original masked over everything outside the city (`addMask.js` + `outline-full.js` are therefore unneeded and were not ported). Live layers kept exactly: ward ground tint (`addGround.js`), ward outlines (`addWards.js`), and fill-extrusion boxes per permit (`addBuildings.js`, same hScale/makeBox/colors). Camera kept (pitch 55, bearing 5, zoom 11.1, maxBounds); `projection: 'globe'` dropped — imperceptible at this zoom. addTTC was commented out in the original and skipped. Old full-viewport map is now a 700px card; gray subtitle line added (original had none).
