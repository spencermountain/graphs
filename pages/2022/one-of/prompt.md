Converted from the 2022 svelte original in public/2022/one-of. The old raw-html `<select>`-inside-title + onchange hack became a plain v-model select in the h1; the old shared Page/Head/Foot shell was replaced with the repo's standard header/card/footer; scoped classes `.row`/`.col` were renamed `.frow`/`.fcol` to avoid clashing with main.css utilities. Colors, sizes, grid logic, and the random lighter-blue boxes are copied exactly.
