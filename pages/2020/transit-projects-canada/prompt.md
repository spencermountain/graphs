Converted from the 2020 svelte/js original in public/2020/transit-projects-canada. Ported the somehow-timeline pieces (Timeline/Axis/Column/Text/Label) as inlined markup+css from the build-time versions in build/bundle.js sourcemaps. `end="today"` is pinned to Dec 30 2020 (the post's era) so bars stay inside the 700px chart. Labels had no `end`, so every bar runs to the timeline bottom, as in the original. Dropped as no-ops in the compiled build: the empty Column `label`/`color` props, the Column margin (its inline declaration was invalid CSS), and Label's isTiny font (overridden by the inline size).
