Converted from the 2020 browserify/somehow-graph original in public/2020/ontario-covid.
Ported somehow-graph v0.3.6's scale/tick/text-placement math by hand into `somehow.js` (parseInt truncations kept for pixel-fidelity). Dates pinned to Canada/Eastern for SSR determinism (original used the browser's timezone). Dropped the dead github "source" footer link.
