Converted from the 2020 svelte/js original in public/2020/computer-history. Ported the somehow-timeline pieces (Timeline/Axis/Column/Label/Horizontal) as inlined markup+css, using the build-time versions from build/bundle.js sourcemaps (the current lib source differs). `end="today"` is pinned to the chart's fixed end, Dec 30 2020, so bars don't overflow the 700px chart in later years. The `width`/`dotted` props on Labels were no-ops in the compiled build and are dropped.
