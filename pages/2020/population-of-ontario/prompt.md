Converted from the 2020 svelte original in public/2020/population-of-ontario. Ported the somehow-sankey layout math into sankey.js (0-indexed cols; skipped unused dots/append/labels/percent features). Styles follow the newer Sankey build this page actually shipped with (20px values, tiny threshold 75px), not the older stale bundle.css. Added a resize re-layout the original lacked. 'Catamaran' font falls back to sans-serif. Kept the original 'Missisauga'/'Vaughn' spellings.
