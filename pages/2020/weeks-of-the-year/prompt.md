Converted from the 2020 svelte/js original in public/2020/weeks-of-the-year. Notes: the original's month-color map had a 'febuary' typo, so February weeks rendered with the css `steelblue` fallback — preserved deliberately (`february: 'steelblue'`). The old Page header title/sub props were empty at runtime; the h1 uses the manifest label "Weeks of the Year". getWeeks.js copied verbatim (spacetime week-start default is Monday in both old and current versions).
